#include <iostream>
#include "ycsb/ycsbc.h"
#include "server.h"

int main(const int argc, const char *argv[]) {
    DoYcsbTest(argc, argv);
//    Server *server = Server::getInstance();
//    server->test();
    return 0;
}
